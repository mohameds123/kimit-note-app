import 'package:flutter/material.dart';

class ColorsManager {

  static const Color white = Colors.white;
  static const Color primaryColor  = Color.fromRGBO(18, 3, 17, 1);
  static const Color txtFeildColor = Color.fromRGBO(232, 89, 230, 0.08);
}